# Project Rules & Guidelines

- **Python Version & Command Launcher**: On Windows in this workspace, always use `py -3.11`  (instead of `python`) when executing Python commands, background tasks, unit tests, or server scripts, as system default `python` defaults to Python 2.7.
